apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  annotations:
    metallb.universe.tf/loadBalancerIPs: ${SECRET_LAN_PREFIX}.11
spec:
  ports:
    - port: 3001
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: uptime-kuma
