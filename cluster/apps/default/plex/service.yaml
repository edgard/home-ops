apiVersion: v1
kind: Service
metadata:
  name: plex
  labels:
    app: plex
  annotations:
    metallb.universe.tf/loadBalancerIPs: 192.168.1.9
spec:
  ports:
    - port: 32400
  type: LoadBalancer
  selector:
    app: plex
