apiVersion: v1
kind: Service
metadata:
  name: minio
spec:
  ports:
    - name: minio-api
      port: 9000
      targetPort: minio-api
    - name: minio-console
      port: 9001
      targetPort: minio-console
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: minio
