---
- name: Generate Talos configuration
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Generate Talos configuration
      ansible.builtin.include_role:
        name: talos
        tasks_from: generate
