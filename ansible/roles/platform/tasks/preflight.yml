---
- name: Require Bitwarden access token
  ansible.builtin.assert:
    that:
      - platform_bitwarden_access_token | length > 0
    fail_msg: BWS_ACCESS_TOKEN is required for platform bootstrap.
  no_log: true
