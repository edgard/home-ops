---
- name: Install packages
  ansible.builtin.apt:
    name:
      - restic
    state: present

- name: Configure backup
  ansible.builtin.cron:
    name: Backup daily
    minute: "0"
    hour: "6"
    user: root
    job: 'RESTIC_REPOSITORY="{{ restic_repo }}" RESTIC_PASSWORD="{{ restic_repo_pass }}" restic backup --host {{ ansible_hostname }} "{{ backup_path }}"{% if heartbeat_url is defined %} && curl -fsS --retry 5 -o /dev/null "{{ heartbeat_url }}"{% endif %}'
