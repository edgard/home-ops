---
- name: Fact the remote user
  set_fact: fixup_remote_user="{{ ansible_user }}"

- name: Add github personal public key to authorized_keys
  authorized_key:
    user: "{{ fixup_remote_user }}"
    state: present
    key: "{{ lookup('url', 'https://github.com/' ~ github_user ~ '.keys') }}"
