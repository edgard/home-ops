---
- name: Fact the remote user
  ansible.builtin.set_fact: fixup_remote_user="{{ ansible_user }}"

- name: Add personal public key to authorized_keys
  ansible.posix.authorized_key:
    user: "{{ fixup_remote_user }}"
    state: present
    key: "{{ ssh_public_key }}"
