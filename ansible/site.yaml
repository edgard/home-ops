---
- name: Manage DNS hosts
  hosts: dns_servers
  roles:
    - config_ssh_keys
    - config_sudo_nopass
    - config_timezone
    - remove_common
    - setup_dns_server
  tags: ["dns"]

- name: Manage KUB hosts
  hosts: kub_servers
  roles:
    - config_ssh_keys
    - config_sudo_nopass
    - config_timezone
    - config_disable_swap
    - remove_snapd
    - remove_common
    - install_backup
    - install_common_vm
    - setup_kub_server
  tags: ["kub"]

- name: Manage DEVL hosts
  hosts: devl_servers
  roles:
    - config_ssh_keys
    - config_sudo_nopass
    - config_timezone
    - remove_snapd
    - remove_common
    - install_backup
    - install_docker
    - install_common_vm
    - setup_devl_server
  tags: ["devl"]
