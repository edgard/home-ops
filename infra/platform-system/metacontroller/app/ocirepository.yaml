---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: metacontroller
  namespace: platform-system
spec:
  interval: 15m
  url: oci://ghcr.io/metacontroller/metacontroller-helm
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: "4.12.5"
