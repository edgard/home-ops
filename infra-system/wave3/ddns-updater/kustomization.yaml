apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: ddns-updater
resources:
  - deployment.yaml
configMapGenerator:
  -  name: ddns-updater-config
     files:
        - configs/config.json
