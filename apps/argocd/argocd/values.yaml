---
fullnameOverride: argocd

dex:
  enabled: false

notifications:
  enabled: false

applicationSet:
  enabled: true
  metrics:
    enabled: false
  podAnnotations:
    reloader.stakater.com/auto: "true"

controller:
  metrics:
    enabled: false
  podAnnotations:
    reloader.stakater.com/auto: "true"

server:
  metrics:
    enabled: false
  podAnnotations:
    reloader.stakater.com/auto: "true"

repoServer:
  metrics:
    enabled: false
  podAnnotations:
    reloader.stakater.com/auto: "true"
