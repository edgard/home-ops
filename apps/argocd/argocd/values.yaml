---
fullnameOverride: argocd

dex:
  enabled: false

notifications:
  enabled: false

applicationSet:
  enabled: true
  podAnnotations:
    reloader.stakater.com/auto: "true"

controller:
  podAnnotations:
    reloader.stakater.com/auto: "true"

server:
  podAnnotations:
    reloader.stakater.com/auto: "true"

repoServer:
  podAnnotations:
    reloader.stakater.com/auto: "true"
