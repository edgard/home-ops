---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restic-repo
  namespace: selfhosted
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Ti
  storageClassName: ""
  volumeName: restic-repo
