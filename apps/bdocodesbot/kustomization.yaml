apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: bdocodesbot
resources:
  - deployment.yaml
  - pvc.yaml
configMapGenerator:
  -  name: bdocodesbot-config
     files:
        - configs/config.yml
