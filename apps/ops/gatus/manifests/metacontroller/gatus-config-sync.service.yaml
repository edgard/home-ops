---
apiVersion: v1
kind: Service
metadata:
  name: gatus-config-sync
  namespace: ops
  labels:
    app.kubernetes.io/name: gatus-config-sync
    app.kubernetes.io/part-of: gatus
spec:
  selector:
    app.kubernetes.io/name: gatus-config-sync
  ports:
    - name: http
      port: 80
      targetPort: http
