apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: clc
resources:
  - deployment.yaml
configMapGenerator:
  -  name: clc-config
     files:
        - configs/config.yml
