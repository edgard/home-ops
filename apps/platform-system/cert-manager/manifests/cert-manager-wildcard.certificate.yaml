---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-manager-wildcard
  namespace: platform-system
spec:
  secretName: cert-manager-wildcard-tls
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-issuer-production
  dnsNames:
    - "*.edgard.org"
    - edgard.org
