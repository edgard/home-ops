---
strategy:
  type: Recreate
installCRDs: true
global:
  leaderElection:
    namespace: platform-system
webhook:
  strategy:
    type: Recreate
cainjector:
  strategy:
    type: Recreate

prometheus:
  enabled: false
