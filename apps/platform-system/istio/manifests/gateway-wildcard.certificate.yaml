---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-wildcard
  namespace: platform-system
spec:
  secretName: gateway-wildcard-tls
  issuerRef:
    kind: Issuer
    name: gateway-issuer-staging
  dnsNames:
    - "*.edgard.org"
    - edgard.org
