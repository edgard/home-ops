apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: smokeping
resources:
  - service.yaml
  - ingress.yaml
  - deployment.yaml
  - pvc.yaml
configMapGenerator:
  - name: smokeping-config
    files:
      - configs/Targets
