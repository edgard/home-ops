apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-system-cloudflared
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '-4'
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-system
  sources:
    - repoURL: oci://ghcr.io/bjw-s-labs/helm
      chart: app-template
      targetRevision: 4.4.0
      helm:
        releaseName: cloudflared
        valueFiles:
          - $values/kubernetes/apps/platform-system/cloudflared/values.yaml
    - repoURL: https://github.com/edgard/home-ops.git
      targetRevision: master
      ref: values
    - repoURL: https://github.com/edgard/home-ops.git
      targetRevision: master
      path: kubernetes/apps/platform-system/cloudflared/resources
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
