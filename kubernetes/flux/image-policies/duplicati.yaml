apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: duplicati
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: duplicati
  policy:
    semver:
      range: ">=0.0.0"
