apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: recyclarr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: recyclarr
  policy:
    semver:
      range: ">=0.0.0"
