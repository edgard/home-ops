apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wildcard-edgard-org-tls
  namespace: arc
spec:
  refreshInterval: 4h
  secretStoreRef:
    name: platform-system-wildcard
    kind: ClusterSecretStore
  target:
    name: wildcard-edgard-org-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  dataFrom:
    - extract:
        key: wildcard-edgard-org-tls
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wildcard-edgard-org-tls
  namespace: home-automation
spec:
  refreshInterval: 4h
  secretStoreRef:
    name: platform-system-wildcard
    kind: ClusterSecretStore
  target:
    name: wildcard-edgard-org-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  dataFrom:
    - extract:
        key: wildcard-edgard-org-tls
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wildcard-edgard-org-tls
  namespace: media
spec:
  refreshInterval: 4h
  secretStoreRef:
    name: platform-system-wildcard
    kind: ClusterSecretStore
  target:
    name: wildcard-edgard-org-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  dataFrom:
    - extract:
        key: wildcard-edgard-org-tls
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wildcard-edgard-org-tls
  namespace: web
spec:
  refreshInterval: 4h
  secretStoreRef:
    name: platform-system-wildcard
    kind: ClusterSecretStore
  target:
    name: wildcard-edgard-org-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  dataFrom:
    - extract:
        key: wildcard-edgard-org-tls
