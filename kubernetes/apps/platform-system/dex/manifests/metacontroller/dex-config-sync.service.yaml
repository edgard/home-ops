---
apiVersion: v1
kind: Service
metadata:
  name: dex-config-sync
  namespace: platform-system
  labels:
    app.kubernetes.io/name: dex-config-sync
    app.kubernetes.io/part-of: dex
spec:
  selector:
    app.kubernetes.io/name: dex-config-sync
  ports:
    - name: http
      port: 80
      targetPort: 8080
