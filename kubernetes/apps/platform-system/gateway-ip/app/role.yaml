apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: gateway-ip-updater
  namespace: platform-system
rules:
  - apiGroups:
      - externaldns.k8s.io
    resources:
      - dnsendpoints
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
