apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: arc
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: arc
  driftDetection:
    mode: enabled
  values:
    fullnameOverride: arc
    replicaCount: 1
