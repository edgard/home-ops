---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hajimari-config-sync
  namespace: edge-services
  labels:
    app.kubernetes.io/name: hajimari-config-sync
    app.kubernetes.io/part-of: hajimari
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hajimari-config-sync
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hajimari-config-sync
        app.kubernetes.io/part-of: hajimari
    spec:
      restartPolicy: Always
      serviceAccountName: hajimari-config-sync
      containers:
        - name: webhook
          image: python:3.14-alpine
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              set -euo pipefail
              pip install --no-cache-dir --quiet pyyaml
              python /scripts/webhook.py
          env:
            - name: TZ
              value: Europe/Warsaw
            - name: CONFIG_TEMPLATE
              value: /templates/base.yaml
            - name: CONFIG_STATIC
              value: /templates/static-apps.yaml
            - name: CONFIG_BOOKMARKS
              value: /templates/static-bookmarks.yaml
            - name: ROUTE_SELECTOR
              value: hajimari.edgard.org/enabled=true,external-dns.edgard.org/scope=external
            - name: WEBHOOK_PORT
              value: "8080"
            - name: RESYNC_SECONDS
              value: "120"
            - name: OUTPUT_CONFIGMAP
              value: hajimari-config-generated
            - name: OUTPUT_KEY
              value: config.yaml
            - name: LOG_LEVEL
              value: INFO
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          volumeMounts:
            - name: scripts
              mountPath: /scripts
              readOnly: true
            - name: templates
              mountPath: /templates
              readOnly: true
      volumes:
        - name: scripts
          configMap:
            name: hajimari-config-sync-scripts
            defaultMode: 0444
        - name: templates
          configMap:
            name: hajimari-config-defaults
            defaultMode: 0444
