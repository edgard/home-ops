---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gatus-config-sync
  namespace: ops
  labels:
    app.kubernetes.io/name: gatus-config-sync
    app.kubernetes.io/part-of: gatus
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gatus-config-sync
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gatus-config-sync
        app.kubernetes.io/part-of: gatus
    spec:
      restartPolicy: Always
      serviceAccountName: gatus-config-sync
      containers:
        - name: webhook
          image: python:3.14-alpine
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              set -euo pipefail
              pip install --no-cache-dir --quiet pyyaml
              python /scripts/webhook.py
          env:
            - name: TZ
              value: Europe/Warsaw
            - name: CONFIG_TEMPLATE
              value: /templates/base.yaml
            - name: CONFIG_STATIC
              value: /templates/static-endpoints.yaml
            - name: SERVICE_SELECTOR
              value: gatus.edgard.org/enabled=true
            - name: WEBHOOK_PORT
              value: "8080"
            - name: RESYNC_SECONDS
              value: "120"
            - name: OUTPUT_CONFIGMAP
              value: gatus-config-generated
            - name: OUTPUT_KEY
              value: config.yaml
            - name: LOG_LEVEL
              value: INFO
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          volumeMounts:
            - name: scripts
              mountPath: /scripts
              readOnly: true
            - name: templates
              mountPath: /templates
              readOnly: true
      volumes:
        - name: scripts
          configMap:
            name: gatus-config-sync-scripts
            defaultMode: 0444
        - name: templates
          configMap:
            name: gatus-config-defaults
            defaultMode: 0444
